/** Must match services/api max attachment count (attachments.rs). */
export const MAX_ATTACHMENTS = 3;

/** Must match services/api default MAX_ATTACHMENT_BYTES (config.rs). */
export const MAX_ATTACHMENT_BYTES = 10 * 1024 * 1024;
